server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/drink_mall?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root123456
  data:
    redis:
      host: localhost
      port: 6379
      password: ""

sa-token:
  is-log: true

wx:
  pay:
    callback-verifier: mock

logging:
  level:
    com.drinkmall: debug
